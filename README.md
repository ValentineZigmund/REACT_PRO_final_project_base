# homework-react-21951242

## Архитектура и структура — 4 балла

### shared/ui только презентационные компоненты, без стора и API (2)
в папке shared/ui остались только компоненты без бизнес логики (исключение - ButtonBack, в нем можно было вынести логику отдельно, но это нарушает принципы DRY & KISS и просто неудобно)

### Изоляция фич по папкам, слои shared/, features/, pages/ соблюдены (1)
компоненты с бизнес логикой перенесены в папки features & entities

### Устранены лишние prop-chains (1)
из компонента CardList Удален лишний prop

## Оптимизация рендеров — 4 балла

### Profiler: найден hotspot и описан кратко (1)
Проблемный компонент - Header, рендерится каждое изменение сущности CartItem. см. Hotspot.png

### Уместный React.memo (1.5)
Из компонента CardItem убран хэндлер удаления и вынесен выше на уровень, компонент мемоизирован и хэндлер обернут в коллбэк

### Точечные useMemo/useCallback для тяжелых вычислений/колбэков в списках (1.5)
см. выше (проблема лишних ререндеров в приложении связана с тем, что любое изменение состояния сущности продукта ведет к пересозданию списка продуктов, что буквально ререндирит все приложение и необходимо концептуально изменить подход к хранению данных в сторе или использовать mobx)

## React.Portal (модалка, доступность) — 3 балла

### Портал работает, #modal-root добавлен, ESC/overlay обрабатываются (2)
добавлен портал на странице SignIn

### Корректный фокус: установка при открытии, возврат на триггер (1)
не сделал в коде (для решения этой задачи можно использовать document.activeElement внутри модалки, записывать в локальное состояние ref и вызывать focus() при закрытие модалки)

## useRef — 2 балла

### Хранение состояния между рендерами без лишних рендеров (1)
внутрь модального окна добавлен счетчик который не приводит к ререндерам

### Автофокус/DOM-интеракция (1)
добавлен автофокус на страницу с логином

## Альтернативная сборка (esbuild/SWC/Vite) — 2 балла

### Рабочая сборка (1)
добавлен сборщик VITE и плагины для работы с react и svg

### Сравнение с Webpack в README (время/размер, краткий вывод) (1)
скриншоты viteBuildTime.png webpackBuildTime.png Вывод - vite быстрее

## React 19 hooks (useActionState/useOptimistic) — 3 балла

### Реальный UX-кейс с одним из хуков (2)
добавлен хук useActionState в reviewForm

### Обработка ошибок/ресет/оптимистичность (1)
после отправки формы на сервер осуществляется ресет

## README и демо — 2 балла

readme добавлен, скриншоты сборок добавлены

